201126

오늘 할일/한일
- net::ERR_INCOMPLETE_CHUNKED_ENCODING 200
에러 뭣때문인지 죽어도 알아야겟음 
-고쳤어!!!!고쳤다!!!~!! 우앙ㅇ아아ㅏ앙


알게 된 것
- let session = '<%=(String)session.getAttribute("loginUser")%>';
요 줄때문에 그런 에러가 났었다.
이것만없으면 아주 잘~돌아간다.
왜지 ㅜㅜ
코드가.. 스크립트코드가 아니라서
그런가?
..
- 암튼 저 코드때문에 제대로 스크립트가 로드 안되는건 10000%맞음
저 한 줄 만 없으면 다 잘됨.
그래서 저거 대신 다른방법ㅂ을 써야됨.

생각해보니 엄과외때로 저렇게 쓰지않고 다른 방법을 썼었던거같음.
일단 난 첨에 c set으로 로긴세션을 set 해서 script에 불러오려고햇다.
근데 안되는거임... name이라던가 id라던가 있어야되는데 c set에는 그런게업음 ㅠ
그래서 그게 가능한 input 박스에다가 value로 넣어줌. 그뒤에 hidden 타입해주면
안보임.
그렇게 name을 만들고 script에다가 name써서 불러올수있었떤거임~~
그래서 session값을 가져오고 세션값이 없으면(console log로 찍어보니 그냥 아무값도
없이 나와서 session="" 이게 로긴이 안됐단말) 로그인하라고 창이 뜨게
잘 만들수있었음!! 끝! 오늘은 이걸로 끄으으으으읕
