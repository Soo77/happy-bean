200914

오늘한일
- header footer 떼기
- app 떼기
- css/main/ 폴더 만들기

알게 된 것
- app 떼려면 config 안에있는 AppWebApplicationInitializer 에서
getServletMappings 함수를 바꿔줬다. "/"으로. *별도 다 뗐다.
- 그리고 css js 등등 안에 main 폴더를 만들고 그 안에 css 파일들을 넣어줬는데 경로를.. 못찾는거다.
왜그런가 했더니 web.xml에 이런걸 써줘야 한다고 한다.
<servlet-mapping>
        <servlet-name>default</servlet-name>
        <url-pattern>*.js</url-pattern>
        <url-pattern>*.css</url-pattern>
        <url-pattern>*.jpg</url-pattern>
        <url-pattern>*.gif</url-pattern>
        <url-pattern>*.png</url-pattern>
        <url-pattern>*.ico</url-pattern>
        <url-pattern>*.swf</url-pattern>
        <url-pattern>*.ttf</url-pattern>
        <url-pattern>*.woff</url-pattern>
    </servlet-mapping>

이걸 써줬더니 잘 됐다.

후기
- 오늘 너무 놀며놀며 했다.. 첫 재택근무(재택근무라고 쓰고 컴앞에 앉아있기라고읽기)
라서 그런지 앉아있는 것도 일이었다.